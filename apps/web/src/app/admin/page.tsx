export default function AdminHomePage() {
  return (
    <p className="text-black/60 dark:text-white/60">
      Pick something from the nav above. This is intentionally bare — real
      admin UI (product listing, editing, order management) comes later;
      what&apos;s here just proves the endpoints and the upload flow work.
    </p>
  );
}
